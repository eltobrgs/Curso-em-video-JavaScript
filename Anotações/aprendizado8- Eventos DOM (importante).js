// Lista de eventos DOM comuns e suas descrições

// Evento de clique: Dispara quando um elemento é clicado
element.addEventListener('click', function(event) {
    // Código a ser executado quando o elemento é clicado
});

// Evento de mouseover: Dispara quando o ponteiro do mouse é movido sobre um elemento
element.addEventListener('mouseover', function(event) {
    // Código a ser executado quando o ponteiro do mouse é movido sobre o elemento
});

// Evento de keydown: Dispara quando uma tecla é pressionada
element.addEventListener('keydown', function(event) {
    // Código a ser executado quando uma tecla é pressionada
});

// Evento de submit: Dispara quando um formulário é enviado
element.addEventListener('submit', function(event) {
    // Código a ser executado quando um formulário é enviado
});

// Evento de load: Dispara quando um recurso e seus recursos dependentes terminaram de carregar
window.addEventListener('load', function(event) {
    // Código a ser executado quando a página termina de carregar
});

// Evento de scroll: Dispara quando o usuário rola a página
window.addEventListener('scroll', function(event) {
    // Código a ser executado quando o usuário rola a página
});

// Evento de resize: Dispara quando a janela é redimensionada
window.addEventListener('resize', function(event) {
    // Código a ser executado quando a janela é redimensionada
});

// Evento de focus: Dispara quando um elemento recebe foco
element.addEventListener('focus', function(event) {
    // Código a ser executado quando o elemento recebe foco
});

// Evento de blur: Dispara quando um elemento perde o foco
element.addEventListener('blur', function(event) {
    // Código a ser executado quando o elemento perde o foco
});

// Evento de change: Dispara quando o valor de um elemento é alterado
element.addEventListener('change', function(event) {
    // Código a ser executado quando o valor do elemento é alterado
});

// Evento de input: Dispara quando um elemento de entrada é alterado
element.addEventListener('input', function(event) {
    // Código a ser executado quando o elemento de entrada é alterado
});
